version: "3.8"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
    volumes:
      - ./data:/app/data
      - ./models:/app/models
      - ./generated:/app/generated
    restart: unless-stopped

